# NVIDIA Granary ASR Proxy Requirements
# Core NeMo framework for Parakeet-tdt model
nemo-toolkit[asr]==1.22.0

# FastAPI and web server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Audio processing
soundfile==0.12.1
librosa==0.10.1
torch>=2.0.0
torchaudio>=2.0.0

# Language detection (fallback)
pycld3==0.22

# Monitoring and metrics
prometheus-client==0.19.0
structlog==23.2.0

# GPU acceleration (if available)
nvidia-ml-py3==7.352.0

# Dependencies for NeMo
hydra-core>=1.2.0
omegaconf>=2.1.0
pytorch-lightning>=1.9.0

# Audio format support
scipy>=1.7.0
numpy>=1.21.0

# Async support
aiofiles==23.2.1

# Testing dependencies (for future tests)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2