"""
PMS Connector Adapters

This directory contains vendor-specific implementations of the PMSConnector protocol.
Each adapter handles the 20% vendor-specific logic while leveraging the 80% common
functionality from the base connector.
"""

# Adapters are dynamically loaded by the factory - no explicit imports needed
