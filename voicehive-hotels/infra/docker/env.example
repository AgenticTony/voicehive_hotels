# Local Docker Compose environment variables (example)
# Copy this file to .env in the same directory and set secure values.
# The .env file will be read automatically by docker compose.

# Grafana
GF_SECURITY_ADMIN_PASSWORD=changeme

